<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Physical Game Jams</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">

</head>
<body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>

    <div class="container">
        <div class="row text-center pb-4">
            <h1 class="display-4">Physical Game Jams</h1>
        </div>

        <div class="row">
            <table class="table table-striped">
                <tr>
                    <th>Name</th>
                    <th>Start Date</th>
                    <th>Due Date</th>
                    <th>Host(s)</th>
                    <th>Hashtag</th>
                </tr>
                {%- for jam in jams %}
                <tr>
                    <td><a href="{{ jam.url() }}">{{ jam.name }}</a></td>
                    <td>{{ jam.start.strftime("%B %d, %Y at %I:%M %p") }}</td>
                    <td>{{ jam.end().strftime("%B %d, %Y at %I:%M %p") }}</td>
                    <td>{%- for owner in jam.owners -%}
                        <a href="https://{{ owner }}.itch.io/">{{ jam.owners[owner] }}</a>
                        {{- ", " if not loop.last else "" -}}
                        {%- endfor -%}</td>
                    <td>{%- if jam.hashtag -%}
                            <a href="https://twitter.com/hashtag/{{ jam.hashtag[1:] }}">{{ jam.hashtag }}</a>
                        {%- else -%}
                            &nbsp;
                        {%- endif -%}</td>
                </tr>
                {%- endfor %}
            </table>
        </div>
    </div>
    
</body>
</html>
